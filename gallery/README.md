# Powah visual gallery

Copy the datapack into a disposable 1.21.1 world, run
`/function powah_gallery:build`, then `/function powah_gallery:pose`.
The entire gallery stays inside `x=199..229`, `y=99..116`, `z=199..217`.
